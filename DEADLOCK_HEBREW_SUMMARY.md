# סיכום פרויקט DEADLOCK

## תקציר מנהלים

DEADLOCK הוא אבטיפוס למערכת AI בזמן אמת לזיהוי, נעילה, מעקב והכוונת בסיס פיזי לעבר מטרה. המערכת מקבלת וידאו ממצלמה, מזהה אנשים, מאפשרת למפעיל לבחור מטרה בלחיצה, עוקבת אחרי אותה מטרה, מנסה לזהות אותה מחדש אם היא נעלמת, מחשבת את שגיאת הכיוון בין הלייזר למטרה, ושולחת פקודות לארדואינו שמזיז סרווים ושולט בלייזר.

הרעיון המרכזי הוא לפתור מצב של איבוד מטרה בסביבה מורכבת: גם אם האדם יוצא זמנית מהפריים, מוסתר, או מתערבב עם אנשים אחרים, המערכת מנסה לשמור את "חתימת הזהות" שלו ולרכוש אותו מחדש.

## זרימת העבודה הכללית

1. המצלמה מספקת פריים וידאו בזמן אמת.
2. YOLO מזהה אנשים בפריים.
3. המשתמש לוחץ על האדם הרצוי.
4. מודל Re-ID יוצר חתימה ויזואלית של האדם שנבחר.
5. ByteTrack עוקב אחרי אותו אדם בין פריימים.
6. SAM2, אם מופעל, מפלח את גוף המטרה ומחשב מרכז מדויק יותר.
7. שכבת הגיאומטריה מחשבת את המרחק בפיקסלים בין הלייזר למטרה.
8. הבקר מתרגם את השגיאה לפקודות זווית לסרווים.
9. הפקודות נשלחות לארדואינו דרך Serial.
10. אם המטרה נאבדת, המערכת עוברת למצב LOST ומנסה לזהות אותה מחדש.

## מבנה הפרויקט

### קבצי כניסה ראשיים

`main.py` הוא קובץ ההרצה הראשי של המערכת. הוא מחבר את הארדואינו, טוען את מודולי הראייה והבקרה, פותח מצלמה, מעדכן את המעקב בכל פריים, מחשב תנועות, שולח פקודות לסרווים, מציג HUD, ומכבה את המערכת בצורה מסודרת בסיום.

`demo_main.py` הוא גרסת דמו להצגה. הוא דומה ל-`main.py`, אבל מותאם יותר להדגמה מול קהל: יש החלקת שגיאות, דרישה לנעילה יציבה למשך כמה שניות, זמן ירי ארוך יותר, הבהוב לייזר, ושחרור מטרה אחרי סיום ההדגמה כדי שאפשר יהיה לחזור על התהליך.

`config.py` מרכז את כל הפרמטרים החשובים: פורט ארדואינו, מזהה מצלמה, נתיבי מודלים, ספי ביטחון, גבולות סרווים, שדה ראייה של המצלמה, מיקום בית של הלייזר, קצב תנועה, רדיוס ירי, והתקן הרצה כמו CPU, CUDA או Intel GPU.

`README.md` נותן תיאור כללי של הפרויקט, מבנה התיקיות, טכנולוגיות, צוות, והוראות הרצה. יש בו בעיית קידוד בתווי אייקונים, אבל המידע הטכני ברור.

## מודול הראייה - `vision`

### `vision/tracker.py`

זהו הקובץ המרכזי של שכבת ה-AI. הוא אחראי על זיהוי, נעילה, מעקב, אימות זהות ורכישה מחדש.

המצבים המרכזיים של המערכת:

`SEARCHING`: המערכת מחפשת אנשים בפריים. בשלב זה YOLO מזהה אנשים, אבל אין עדיין מטרה נעולה.

`LOCKED`: המשתמש בחר אדם, והמערכת עוקבת אחריו. ByteTrack שומר מזהה עקיבה, ו-Re-ID בודק מדי פעם שהאדם הנעקב הוא עדיין האדם הנכון.

`LOST`: המערכת איבדה את המעקב. היא ממשיכה לחפש אנשים בפריים ומנסה להשוות אותם לחתימה הוויזואלית של המטרה המקורית.

פונקציות עזר חשובות:

`_iou` מחשבת חפיפה בין שתי תיבות זיהוי. זה עוזר להבין אם שתי תיבות בפריימים שונים כנראה מייצגות את אותו אובייקט.

`_clamp_box` מוודאת שתיבת הזיהוי נשארת בתוך גבולות התמונה.

`_to_numpy` ממירה פלטים של PyTorch או Ultralytics למערכי NumPy.

`_openvino_dir` מוצאת תיקיית מודל YOLO שיוצא ל-OpenVINO.

`_load_yolo` טוענת את YOLO. אם המערכת מוגדרת להרצה על Intel, היא משתמשת במודל OpenVINO, ואם אין כזה היא מנסה לייצא אותו.

### מחלקת `_ReIDBackbone`

זוהי מחלקה פנימית שמשתמשת ב-`MobileNet_V3_Small` כמודל לחילוץ מאפיינים. היא לא מסווגת את האדם לשם מסוים, אלא מייצרת וקטור מספרי שמייצג את המראה שלו.

הווקטור הזה נקרא embedding. ככל ששני embeddings דומים יותר, כך יש סיכוי גבוה יותר שמדובר באותו אדם.

### מחלקת `SentryTracker`

`__init__` טוענת את המודלים, בוחרת התקן הרצה, מאתחלת את YOLO, MobileNet Re-ID, SAM2 אם מופעל, ואת משתני המצב.

`get_aim_target` מחזירה את מרכז המטרה הנוכחי אם יש מטרה נעולה או אבודה עם מיקום אחרון ידוע.

`consume_aim_reset` מודיעה לבקר שצריך לאפס את ספירת יציבות הכיוון.

`_resolve_devices` בוחרת אם להריץ על CPU, CUDA או Intel/OpenVINO.

`_reid_bbox` חותכת בעיקר את פלג הגוף העליון, כי בגדים וצורת גוף עליונה עוזרים לזהות אדם מחדש.

`_embed_crop` יוצרת embedding מתוך crop של אדם.

`_box_center` מחשבת מרכז של תיבת זיהוי.

`_center_dist` מחשבת מרחק בין מועמד לבין המיקום האחרון של המטרה.

`_reacquire_radius` מגדילה את רדיוס החיפוש ככל שהמטרה אבודה יותר זמן.

`_person_score` משווה embedding של מועמד לחתימה המקורית ולגלריית חתימות שנאספה לאורך הזמן.

`_accept_person` מחליטה אם הדמיון מספיק גבוה כדי לקבל מועמד כרכישה מחדש.

`_verify_identity` בודקת בזמן נעילה שהאדם הנעקב עדיין מתאים לחתימת המטרה.

`_pick_track_index` בוחרת איזה track ID של ByteTrack שייך למטרה שנבחרה.

`_find_lost_match` מחפשת, במצב LOST, את האדם הכי דומה למטרה המקורית.

`_add_to_gallery` מוסיפה חתימות חדשות של אותה מטרה לגלריה, כדי לשפר עמידות לשינויי זווית, תאורה ותנוחה.

`_seed_gallery` מאתחלת את הגלריה עם החתימה הראשונה של המטרה.

`_sam_centroid` מריצה SAM2 על תיבת המטרה ומחשבת את מרכז המסכה.

`_target_center` מחליטה האם להשתמש במרכז תיבת הזיהוי או במרכז המסכה של SAM2.

`set_target` נועלת מטרה לפי תיבת זיהוי.

`set_target_from_click` נועלת מטרה לפי לחיצת המשתמש על המסך.

`_boxes_from_result` מחלצת תיבות זיהוי ומזהי עקיבה מתוצאת YOLO.

`update` היא פונקציית הליבה. היא מתבצעת בכל פריים ומנהלת את המעבר בין SEARCHING, LOCKED ו-LOST.

`release_target` משחררת את המטרה וחוזרת למצב חיפוש.

### `vision/ui_manager.py`

קובץ זה אחראי על הצגת הממשק הוויזואלי מעל הווידאו.

הוא מצייר:

- סטטוס מערכת: SEARCHING, LOCKED, LOST או FIRING.
- כוונת לייזר.
- סימון מטרה.
- קו בין נקודת הלייזר למרכז המטרה.
- תצוגת שגיאה בפיקסלים.
- פקודת תנועה אחרונה.
- זוויות סרוו.
- מסכת SAM2 אם קיימת.
- התראת ירי בזמן הדגמה.

הפונקציה `mouse_callback` מטפלת בלחיצת עכבר ומעבירה אותה ל-`SentryTracker` כדי לנעול מטרה.

## מודול הבקרה - `control`

### `control/aim_geometry.py`

מודול זה מתרגם בין עולם התמונה לעולם הזוויות של הסרווים.

`home_px` מגדירה איפה הלייזר אמור להופיע בתמונה כאשר הסרווים בזוויות הבית.

`px_per_deg_h` מחשבת כמה פיקסלים בתמונה מייצגים מעלה אחת בציר האופקי.

`px_per_deg_v` מחשבת כמה פיקסלים מייצגים מעלה אחת בציר האנכי.

`laser_pixel` חוזה איפה הלייזר נמצא בתמונה לפי זוויות הסרוו.

`pixel_error` מחשבת את השגיאה בין מרכז המטרה לבין נקודת הלייזר.

`error_to_servo_delta` מתרגמת שגיאת פיקסלים לשינוי זווית בסרוו.

### `control/controller.py`

הבקר מחליט כמה להזיז את הסרווים ומתי מותר להחשיב את הכיוון כיציב.

`servo_angles` מחזירה את הזוויות המשוערות הנוכחיות.

`update_actual_angles` מדמה את מהירות התנועה הפיזית של הסרוו, כדי שהמערכת לא תניח שהסרוו הגיע מיד לזווית החדשה.

`laser_pixel` מחזירה את מיקום הלייזר הצפוי בתמונה.

`pixel_error` מחשבת שגיאת כיוון.

`_clamp_delta` מונעת פקודה שתוציא את הסרוו מחוץ לגבולות המותרים.

`_max_cmd_for_err` מאפשרת תנועה גדולה יותר כשהשגיאה גדולה.

`_quantize_cmd` הופכת שינוי זוויתי רציף לפקודת תנועה שלמה ומוגבלת.

`compute_move` היא פונקציית ההחלטה המרכזית לתנועה. היא מקבלת שגיאת פיקסלים ומחזירה פקודות `H` ו-`V`.

`reset_home` מחזירה את המערכת לזוויות הבית.

`reset_aim` מאפסת את ספירת היציבות.

`validate_fire` בודקת שהמטרה נשארה בתוך רדיוס הירי במשך מספר פריימים רצופים.

### `control/serial_sender.py`

מודול זה אחראי לתקשורת עם הארדואינו.

`connect` פותחת חיבור Serial.

`_write` שולחת פקודה כטקסט ASCII.

`send_move` שולחת תנועות יחסיות בצירים H ו-V.

`send_goto` שולחת זוויות יעד מוחלטות.

`send_reset` שולחת פקודת איפוס.

`send_laser_state` מדליקה או מכבה לייזר.

`send_fire` שולחת פקודת ירי.

`homing` מחזירה את הצריח לזוויות הבית.

`startup` מבצעת התחלה בטוחה: לייזר כבוי וחזרה לבית.

`shutdown` מכבה לייזר ומחזירה את הצריח לבית.

`close` סוגרת את החיבור.

## מודול Arduino

### `arduino/sentry/sentry.ino`

זהו קוד המיקרו-בקר. הוא מקבל פקודות מהמחשב ומפעיל שני סרווים ולייזר.

פקודות נתמכות:

- `H <delta>`: תנועה יחסית אופקית.
- `V <delta>`: תנועה יחסית אנכית.
- `G <h> <v>`: מעבר לזוויות מוחלטות.
- `RESET`: חזרה לבית.
- `LASER_ON`: הדלקת לייזר.
- `LASER_OFF`: כיבוי לייזר.
- `FIRE`: הבהוב לייזר קצר.
- `HS <delta>`: תנועה אופקית חלקה.
- `VS <delta>`: תנועה אנכית חלקה.

הקוד כולל הגבלת זוויות כדי לא לעבור את גבולות הסרווים.

### `arduino/upload.py`

סקריפט עזר להעלאת הקוד לארדואינו.

`find_arduino_cli` מחפש את Arduino CLI.

`get_connected_board_port` מנסה לזהות אוטומטית את פורט ה-COM של הלוח.

`run_command` מריץ פקודות קומפילציה והעלאה.

`main` מקמפל את הסקץ', מזהה פורט אם צריך, מעלה את הקוד, ואפשר גם לפתוח Serial Monitor.

## מודלים במערכת

### YOLOv8n

YOLOv8n הוא מודל זיהוי אובייקטים קל ומהיר. בפרויקט הוא מוגדר לזהות רק אנשים, כלומר class 0 מתוך COCO.

תפקידו:

- לזהות אנשים בפריים.
- לספק תיבות זיהוי.
- במצב LOCKED, לעבוד עם ByteTrack כדי לעקוב אחרי המטרה.

יתרון מרכזי: מהיר ומתאים לזמן אמת.

חיסרון מרכזי: בגלל שהוא גרסת nano, הוא פחות מדויק ממודלים גדולים יותר.

### ByteTrack

ByteTrack אינו קובץ מודל נפרד, אלא אלגוריתם עקיבה שמשתמש בתוצאות הזיהוי של YOLO.

תפקידו:

- לחבר בין זיהויים בפריימים שונים.
- לתת לכל אדם מזהה עקיבה.
- לעזור למערכת לשמור על אותה מטרה לאורך זמן.

יתרון: מהיר ויעיל.

חיסרון: יכול להחליף זהויות במצבים של הסתרה, צפיפות או מעבר אנשים קרוב.

### MobileNet V3 Small Re-ID

זהו מודל חילוץ מאפיינים. הוא לא נבנה במקור כמודל Re-ID צבאי או ייעודי, אבל בפרויקט משתמשים בו כדי ליצור embedding של האדם.

איך זה עובד:

1. המערכת חותכת את פלג הגוף העליון של האדם.
2. MobileNet מפיק וקטור מאפיינים.
3. הווקטור מנורמל.
4. מועמדים חדשים מושווים לווקטור המקורי בעזרת מכפלה פנימית.
5. אם הדמיון מספיק גבוה, המערכת מניחה שזה אותו אדם.

יתרון: קל, מהיר וזמין.

חיסרון: לא מאומן במיוחד לזיהוי מחדש של אנשים, ולכן פחות אמין בסביבה צפופה או עם אנשים דומים.

### SAM2 Tiny

SAM2 הוא מודל segmentation. בפרויקט משתמשים בגרסת tiny כדי לקבל מסכה של גוף המטרה בתוך תיבת הזיהוי.

תפקידו:

- לשפר את נקודת המרכז של המטרה.
- להציג מסכה על האדם הנעקב.
- לעזור לכוון לפי צורת הגוף ולא רק לפי מלבן הזיהוי.

יתרון: מרכז מדויק יותר מתיבת bounding box רגילה.

חיסרון: מוסיף זמן חישוב, ולכן הקוד מריץ אותו רק כל כמה פריימים.

### OpenVINO

OpenVINO משמש להרצת YOLO בצורה מותאמת לחומרת Intel. יש בפרויקט תיקיית `yolov8n_openvino_model` שמכילה מודל YOLO מיוצא.

יתרון: שיפור ביצועים על Intel GPU/CPU.

חיסרון: מוסיף תלות בסביבת OpenVINO ובתאימות חומרה.

## נקודות חוזקה של הפרויקט

- ארכיטקטורה מודולרית וברורה.
- שילוב חכם של זיהוי, עקיבה, Re-ID וסגמנטציה.
- מתאים להדגמה חיה כי יש גרסת דמו ייעודית.
- יכול לרוץ גם בלי ארדואינו מחובר, מה שעוזר בבדיקות תוכנה.
- תמיכה ב-OpenVINO הופכת את הפרויקט רלוונטי להרצה בקצה.
- קיימות מגבלות תנועה לסרווים, מה שמפחית סיכון מכני.
- יש לוגיקה של רכישה מחדש ולא רק מעקב פשוט.
- גלריית embeddings משפרת התאמה לאורך זמן.

## חסרונות וסיכונים

- מודל Re-ID אינו ייעודי לזיהוי מחדש של בני אדם.
- YOLOv8n מהיר אך פחות מדויק ממודלים גדולים יותר.
- המערכת עלולה להתבלבל בין אנשים דומים.
- כיול הלייזר והמצלמה ידני.
- אין מדידת עומק, ולכן תיקון פרלקסה מוגבל.
- אין משוב אמיתי מהסרווים; Python מעריך את הזווית, אבל לא מקבל אישור מחיישן.
- פרוטוקול Serial פשוט וללא checksum או acknowledgment.
- אין סט בדיקות אוטומטי מלא.
- קובץ הבדיקה הקיים הוא בדיקה ידנית לארדואינו ולא unit test.
- README דורש תיקון קידוד.
- SAM2 מוסיף עומס חישובי.

## שיפורים מומלצים

1. להשתמש במודל Re-ID ייעודי לאנשים, למשל מודל שאומן על Market1501, DukeMTMC או dataset דומה.
2. לבנות תהליך כיול אוטומטי ללייזר, מצלמה וסרווים.
3. להוסיף משוב חומרה מהסרווים, למשל encoder או אישור מצב מהארדואינו.
4. לשפר את פרוטוקול התקשורת עם acknowledgments, checksum וטיפול בשגיאות.
5. להוסיף מצב בטיחות: arming, emergency stop, מגבלת זמן ירי, וכפתור השבתה.
6. להוסיף בדיקות אוטומטיות לגיאומטריה, בקר, גבולות סרוו ומעברי מצבים.
7. להוסיף מדדים בזמן אמת: FPS, confidence, similarity, זמן איבוד מטרה, מספר ניסיונות reacquire.
8. להוסיף מצב הרצה על וידאו מוקלט כדי להדגים בלי תלות בתנאי מצלמה חיים.
9. לשפר README ולהוסיף תרשים ארכיטקטורה.
10. להחליף `COM3` קבוע בזיהוי אוטומטי או בחירה נוחה למשתמש.

## איך להציג את הפרויקט מול Rafael

המסר המרכזי:

DEADLOCK הוא אבטיפוס שמדגים יכולת חשובה: לא רק לזהות מטרה, אלא לשמור עליה לאורך זמן ולנסות לרכוש אותה מחדש אחרי איבוד קשר עין.

נקודות שכדאי להדגיש:

- המערכת בנויה כ-pipeline מלא: מצלמה, AI, גיאומטריה, בקרה וחומרה.
- יש הפרדה טובה בין שכבות, ולכן קל לשדרג מודלים או חומרה.
- Re-ID הוא היתרון המרכזי מול tracker רגיל.
- SAM2 מוסיף דיוק בכיוון.
- OpenVINO מראה מחשבה על הרצה בזמן אמת ועל edge AI.
- הדמו מדגים מערכת עובדת מקצה לקצה, גם אם היא עדיין אבטיפוס.

ניסוח קצר להצגה:

"המערכת שלנו מקבלת וידאו בזמן אמת, מזהה אנשים, מאפשרת למפעיל לבחור מטרה, ומאותו רגע היא לא רק עוקבת אחרי bounding box אלא שומרת חתימת זהות של המטרה. אם המעקב נשבר, המערכת מחפשת מועמדים חדשים ומשווה אותם לחתימה המקורית כדי לרכוש מחדש את אותה מטרה. במקביל, שכבת הבקרה מחשבת את שגיאת הכיוון בין הלייזר למטרה ומזיזה את הסרווים דרך ארדואינו."

## סטטוס בדיקות

ניסינו להריץ בדיקות אוטומטיות עם `pytest`, אבל בסביבה הנוכחית `pytest` לא מותקן, גם לא ב-Python הכללי וגם לא בתוך ה-virtual environment הקיים. לכן כרגע אין תוצאת בדיקות אוטומטיות.

בנוסף, הקובץ `tests/test_sentry.py` הוא למעשה בדיקה ידנית ששולחת פקודות Serial לארדואינו, ולא בדיקת יחידה אוטומטית.

## סיכום

DEADLOCK הוא פרויקט חזק להאקתון כי הוא לא נשאר ברמת אלגוריתם בלבד. הוא מחבר בין מודלי ראייה ממוחשבת, לוגיקת Re-ID, בקרת תנועה, ממשק ויזואלי וחומרה פיזית. היתרון המרכזי שלו הוא הרעיון של reacquisition: היכולת לנסות לזהות מחדש מטרה שאבדה.

כדי להפוך אותו ממערכת דמו למערכת אמינה יותר, צריך בעיקר לשפר כיול, בדיקות, בטיחות, פרוטוקול חומרה ומודל Re-ID ייעודי.
